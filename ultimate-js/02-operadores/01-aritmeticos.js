let a = 5;
let b = 7;

// console.log (a + b, 'adicion');// + corresponde al operador aritmético de "adición"
// console.log (a - b, 'resta');// - corresponde al operador aritmético de "resta"
// console.log (a * b, 'multiplicacion');// * corresponde al operador aritmético de "multiplicación"
// console.log (a / b, 'division');// / corresponde al operador aritmético de "división"
// console.log (a % b, 'modulo');// % corresponde al operador aritmético de "módulo (lo que sobra de una división)"
// console.log (a ** b, 'potencia');// ** corresponde al operador aritmético de "módulo potencia"

//incrementar el valor en uno, si se pone despues ++ primero aparece el valor real y despues se suma, y al final aparece el valor real despues de todas las operaciones
console.log(++a);
console.log(a++);
console.log(a);
//decrementar
console.log(--a);
console.log(a--);
console.log(a);